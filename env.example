# Local demo mode: true = no real MongoDB needed (uses in-memory mock DB)
DEMO_MODE=true

# MongoDB connection string (used only when DEMO_MODE=false)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/

# MongoDB database name
MONGO_DBNAME=let_them_cook
